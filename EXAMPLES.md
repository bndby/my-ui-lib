# Примеры использования CLI

## Сценарий 1: Новый проект с нуля

```bash
# Создать новый React проект
npx create-react-app my-app --template typescript
cd my-app

# Установить библиотеку
npm install my-ui-lib

# Инициализировать CLI
npx my-ui init
# → Путь для компонентов: src/components ✓
# → Путь для хуков: src/hooks ✓
# → Путь для утилит: src/lib ✓
# → Путь для тестовых конфигов: . ✓
# → Настроить тестовое окружение сейчас? (Y/n)

# Выбрать Vitest
# → Выберите тестовый фреймворк:
#   ❯ Vitest (рекомендуется) — быстрый и современный
#     Jest — зрелое и надежное решение
#     Rstest — новый от Rspack
#     Установить все три

# CLI установит:
# ✓ test-setup.ts
# ✓ test-globals.d.ts
# ✓ css-modules.d.ts
# ✓ vitest.config.ts

# Установить зависимости
npm install -D vitest @vitejs/plugin-react @testing-library/react \
  @testing-library/jest-dom @testing-library/user-event jsdom

# Добавить кнопку
npx my-ui add ui/button
# ✓ button.tsx
# ✓ button.module.css
# ✓ button.test.tsx

# Запустить тесты
npm test
```

## Сценарий 2: Добавление в существующий проект с Jest

```bash
cd my-existing-project

# У вас уже есть jest.config.js
# Установить библиотеку
npm install my-ui-lib

# Инициализировать (пропустить настройку тестов)
npx my-ui init
# → Настроить тестовое окружение сейчас? (Y/n) n

# Добавить компонент
npx my-ui add ui/modal
# 🧪 Обнаружены тесты, но тестовое окружение не настроено
# → Установить конфигурацию для тестирования? (Y/n) y

# Выбрать Jest
# → Выберите тестовый фреймворк:
#     Vitest (рекомендуется) — быстрый и современный
#   ❯ Jest — зрелое и надежное решение
#     Rstest — новый от Rspack
#     Установить все три

# CLI установит только то, чего нет:
# ⚠ Файл уже существует: jest.config.js
# ✓ test-setup.ts (новый)
# ✓ test-globals.d.ts (новый)
# ✓ css-modules.d.ts (новый)

# Проверить зависимости (если нужно)
npm install -D @testing-library/react @testing-library/jest-dom \
  @testing-library/user-event

# Запустить тесты
npm test
```

## Сценарий 3: Установка всех трех фреймворков

```bash
# Для монорепозитория или экспериментов
npx my-ui setup-tests --framework all -y

# Установится:
# ✓ vitest.config.ts
# ✓ jest.config.js
# ✓ rstest.config.ts
# ✓ test-setup.ts
# ✓ test-globals.d.ts
# ✓ css-modules.d.ts

# Установить зависимости для всех
npm install -D \
  vitest jest @rstest/core \
  @types/jest \
  @vitejs/plugin-react \
  @testing-library/react \
  @testing-library/jest-dom \
  @testing-library/user-event \
  jest-environment-jsdom \
  ts-jest \
  jsdom

# Использовать любой:
npm run test:vitest   # vitest
npm run test:jest     # jest
npm run test:rstest   # rstest
```

## Сценарий 4: Пакетное добавление элементов

```bash
# Интерактивный выбор
npx my-ui add

# → Выберите элементы для добавления:
#   [ ] ui/button — Универсальный компонент кнопки
#   [x] ui/card — Компонент карточки
#   [x] ui/input — Стилизованный компонент ввода
#   [ ] ui/modal — Модальное окно
#   [x] hooks/use-toggle — Управление boolean состоянием
#   [ ] hooks/use-debounce — Debounce значений
#   [x] lib/cn — Объединение CSS классов
#   ...

# Множественный выбор
npx my-ui add ui/button ui/card ui/input hooks/use-toggle lib/cn -y

# Все элементы сразу
npx my-ui add --all -y
```

## Сценарий 5: Просмотр информации

```bash
# Список всех элементов
npx my-ui list

# 📦 my-ui-lib v1.0.0
#
# Компоненты:
#   ui/button — Универсальный компонент кнопки с вариантами стилей и размеров
#   ui/card — Компонент карточки с заголовком, описанием и контентом
#   ui/input — Стилизованный компонент текстового ввода
#   ui/modal — Модальное окно с overlay и анимацией
#
# Хуки:
#   hooks/use-local-storage — Хук для работы с localStorage
#   ...

# Только компоненты
npx my-ui list --components

# Детальная информация
npx my-ui info ui/modal

# ui/modal
# Модальное окно с overlay и анимацией
#
# Категория: ui
# Файлы:
#   • ui/modal/modal.tsx
#   • ui/modal/modal.module.css
#   • ui/modal/modal.test.tsx
# Зависимости:
#   • hooks/use-click-outside
```

## Сценарий 6: Переустановка тестовых конфигов

```bash
# Если нужно обновить конфиги
npx my-ui setup-tests -f vitest

# → ✓ Тестовое окружение уже настроено!
# → Переустановить конфигурацию? (y/N) y

# Файлы будут перезаписаны:
# ✓ vitest.config.ts (обновлен)
# ✓ test-setup.ts (обновлен)
# ✓ test-globals.d.ts (обновлен)
```

## Сценарий 7: Миграция с Jest на Vitest

```bash
# 1. Установить конфиг Vitest
npx my-ui setup-tests -f vitest -y

# 2. Установить зависимости
npm install -D vitest @vitejs/plugin-react jsdom

# 3. Обновить package.json
# "test": "jest" → "test": "vitest"

# 4. Запустить тесты (работают без изменений!)
npm test

# 5. Удалить старые конфиги (опционально)
rm jest.config.js
npm uninstall jest @types/jest ts-jest
```

## Сценарий 8: Добавление в Rspack проект

```bash
cd my-rspack-project

# Инициализировать
npx my-ui init

# Настроить тесты с Rstest
npx my-ui setup-tests -f rstest -y

# Установить зависимости
npm install -D @rstest/core @vitejs/plugin-react \
  @testing-library/react @testing-library/jest-dom \
  @testing-library/user-event jsdom

# Добавить компоненты
npx my-ui add ui/button ui/card

# Тесты работают с Rstest!
npm test
```

## Структура проекта после установки

```
my-project/
├── src/
│   ├── components/          # Компоненты (ui/*)
│   │   ├── Button.tsx
│   │   ├── Button.module.css
│   │   ├── Button.test.tsx
│   │   ├── Card.tsx
│   │   └── ...
│   ├── hooks/               # Хуки (hooks/*)
│   │   ├── useToggle.ts
│   │   ├── useToggle.test.ts
│   │   └── ...
│   └── lib/                 # Утилиты (lib/*)
│       ├── cn.ts
│       ├── cn.test.ts
│       └── ...
├── vitest.config.ts         # Конфиг Vitest
├── test-setup.ts            # Setup для тестов
├── test-globals.d.ts        # TypeScript типы
├── css-modules.d.ts         # CSS модули типы
├── my-ui.config.json        # Конфигурация CLI
└── package.json
```

## Полезные команды

```bash
# Справка
npx my-ui --help
npx my-ui add --help
npx my-ui setup-tests --help

# Версия
npx my-ui --version

# Алиасы
npx my-ui ls        # вместо list
npx my-ui add -y    # вместо --yes
npx my-ui add -a    # вместо --all
```

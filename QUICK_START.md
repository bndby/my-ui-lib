# 🚀 Быстрый старт

## Установка и первое использование

```bash
# 1. Установить библиотеку
npm install my-ui-lib

# 2. Инициализировать в проекте
npx my-ui init

# 3. CLI автоматически предложит настроить тесты
# Выберите: Vitest / Jest / Rstest

# 4. Добавить компоненты
npx my-ui add ui/button ui/card

# 5. Установить зависимости для тестов (если выбран Vitest)
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom

# 6. Готово! Запустите тесты
npm test
```

## ✨ Основные команды

```bash
# Инициализация
npx my-ui init

# Список доступных элементов
npx my-ui list

# Добавить элементы
npx my-ui add ui/button           # Один элемент
npx my-ui add ui/button ui/card   # Несколько элементов
npx my-ui add                     # Интерактивный выбор
npx my-ui add --all               # Все элементы

# Настройка тестов
npx my-ui setup-tests                 # Интерактивно
npx my-ui setup-tests -f vitest       # Vitest
npx my-ui setup-tests -f jest         # Jest
npx my-ui setup-tests -f rstest       # Rstest
npx my-ui setup-tests -f all          # Все три

# Информация
npx my-ui info ui/button          # Детали элемента
npx my-ui --version               # Версия
npx my-ui --help                  # Справка
```

## 🧪 Автоматическая настройка тестов

### При добавлении элементов

CLI **автоматически** обнаруживает, что добавленные элементы содержат тесты:

```bash
npx my-ui add ui/button

# CLI выведет:
# ✓ button.tsx
# ✓ button.module.css
# ✓ button.test.tsx
#
# 🧪 Обнаружены тесты, но тестовое окружение не настроено
# → Установить конфигурацию для тестирования? (Y/n)
```

### Что устанавливается автоматически

- ✅ `test-setup.ts` — настройка окружения
- ✅ `test-globals.d.ts` — TypeScript типы
- ✅ `css-modules.d.ts` — типы для CSS модулей
- ✅ `vitest.config.ts` / `jest.config.js` / `rstest.config.ts` — конфиг фреймворка

Файлы с расширением `.example.*` копируются с удалением `.example`:
- `vitest.config.example.ts` → `vitest.config.ts`
- `jest.config.example.js` → `jest.config.js`

### Проверка существующих конфигов

CLI **умный** — он проверяет наличие конфигов:

```bash
# Если конфиги уже есть
npx my-ui add ui/card

# CLI не будет предлагать установку тестов
# ✓ Тестовое окружение уже настроено
```

## 📦 Доступные элементы

### UI Компоненты (4)

- `ui/button` — Кнопка с вариантами
- `ui/card` — Карточка контента
- `ui/input` — Текстовый ввод
- `ui/modal` — Модальное окно

### Хуки (5)

- `hooks/use-local-storage` — localStorage
- `hooks/use-debounce` — Debounce
- `hooks/use-media-query` — Media queries
- `hooks/use-click-outside` — Клики вне элемента
- `hooks/use-toggle` — Boolean состояние

### Утилиты (4)

- `lib/cn` — Объединение классов
- `lib/format` — Форматирование
- `lib/validators` — Валидация
- `lib/helpers` — Общие функции

## 🎯 Типичные сценарии

### Новый проект

```bash
npx create-react-app my-app --template typescript
cd my-app
npm install my-ui-lib
npx my-ui init
npx my-ui add ui/button ui/card
npm test
```

### Существующий проект с Jest

```bash
npm install my-ui-lib
npx my-ui init
npx my-ui add ui/button
# При запросе выбрать "Jest"
npm test
```

### Добавить все сразу

```bash
npx my-ui init
npx my-ui add --all -y
npx my-ui setup-tests -f vitest -y
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
npm test
```

## ⚙️ Настройка

Конфигурация сохраняется в `my-ui.config.json`:

```json
{
  "components": "src/components",
  "hooks": "src/hooks",
  "utils": "src/lib",
  "tests": "."
}
```

Измените пути под свою структуру проекта.

## 📚 Дополнительно

- [README.md](./README.md) — Полная документация
- [EXAMPLES.md](./EXAMPLES.md) — Детальные примеры
- [registry/README.tests.md](./registry/README.tests.md) — Документация по тестам

## 💡 Советы

1. **Всегда используйте `init`** перед добавлением элементов
2. **Автоматическая настройка тестов** — просто согласитесь при предложении
3. **Зависимости разрешаются автоматически** — CLI добавит всё необходимое
4. **Тесты универсальны** — работают с Jest, Vitest и Rstest без изменений
5. **Используйте `-y`** для пропуска подтверждений в CI/CD

## 🆘 Проблемы?

```bash
# Переустановить тестовые конфиги
npx my-ui setup-tests -f vitest

# Посмотреть что будет установлено
npx my-ui info ui/button

# Проверить список всех элементов
npx my-ui list

# Справка по команде
npx my-ui add --help
```
